let args = process.argv.slice(2);
for (let i =0 ; i<args.length ; i++)        //for loop that sets the index and constants.
{   console.log(args[i]);                     //prints out arguments on separate lines
}
